---
interface Props {
  kicker: string;
  title: string;
  text: string;
}

const { kicker, title, text } = Astro.props;
---

<div class="section-head">
  <div>
    <span class="kicker">{kicker}</span>
    <h2>{title}</h2>
  </div>
  <p>{text}</p>
</div>
